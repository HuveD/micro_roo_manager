{{ARGUMENTS}} ultrathink, planner with zen. 아래 지침에 따라 계획을 수립하고 자동으로 실행하세요.

**핵심**: ClaudeCode TodoWrite 툴로 모든 작업을 기록하고, `plan-output.md`를 실시간으로 업데이트합니다.

## 🚀 실행 프로세스

### Phase 1: 분석 및 계획 (5분)
1. **현황 파악**
   - CLAUDE.md 확인
   - TodoRead로 기존 작업 확인
   - plan-output.md 확인 (미완료 있으면 중단)
   - docs/ 구조 파악 (새 팀원 온보딩 문서 체계 확인)

2. **계획 수립**
   - **TodoWrite로 모든 작업을 TODO로 등록** (필수)
   - **plan-output.md 생성** (필수)
   - **주기적 업데이트를 위한 리마인더 TODO 추가** (예: "5분마다 plan-output.md 업데이트")
   - 사용자에게 계획 요약 후 "계획대로 진행하겠습니다" 선언

### Phase 2: 자동 실행
**각 TODO 시작 시**:
- TodoWrite로 상태를 'in_progress'로 변경
- plan-output.md에 시작 시간 기록
- 진행 로그 추가

**진행 중 (리마인더 TODO 실행)**:
- 5분마다 plan-output.md 업데이트
- 진행률, 발견 이슈, 자체 해결 사항 기록
- TodoRead로 상태 확인

**완료/실패 시**:
- TodoWrite로 상태 업데이트
- plan-output.md에 결과 기록
- 소요 시간 계산

### Phase 3: 문서 동기화
**새 팀원 관점 문서 관리**:
1. **문서 필요성 판단 기준**
   ```
   핵심 질문: "새 팀원이 프로젝트에 합류했을 때 이 정보가 필요한가?"
   
   필요한 정보 유형:
   - 프로젝트 이해: 목적, 아키텍처, 핵심 개념
   - 개발 시작: 환경 설정, 빌드, 테스트 방법
   - 문제 해결: 트러블슈팅, 자주 하는 실수
   - 협업 방법: 코드 리뷰, 브랜치 전략
   - 도메인 지식: 비즈니스 로직, 용어 정의
   ```

2. **문서 관리 전략**
   - 기존 프로젝트의 문서 구조 파악 및 준수
   - docs/ 디렉토리 내 자율적 구성
   - 새 팀원의 온보딩 경로를 고려한 구조화
   - 코드 변경이 새 팀원의 이해에 영향을 주는 경우 업데이트

3. **문서화 제외 대상**
   - 임시 작업 내용, 일회성 이슈
   - 코드로 충분히 설명되는 구현 세부사항
   - 금방 outdated 될 단기 계획

## 📋 plan-output.md 템플릿

```markdown
# [작업명] 실행 계획 및 진행 상황

## 🎯 작업 개요
### 현재 문제/상태
[문제 상황을 구체적으로 설명. 왜 이 작업이 필요한지, 현재 어떤 문제가 있는지]
예시: "사용자 인증 시스템이 없어서 API가 보안에 취약함. 누구나 모든 엔드포인트에 접근 가능한 상태."

### 목표 상태
[작업 완료 후 달성하고자 하는 상태를 명확히 설명]
예시: "JWT 기반 인증 시스템 구축으로 인가된 사용자만 API 접근 가능하도록 보안 강화"

### 영향 범위
- 영향받는 모듈: [user.service, auth.controller, api.routes]
- 의존성: [jsonwebtoken 패키지 추가, 미들웨어 적용]
- 테스트 범위: [인증 테스트, 기존 API 테스트 수정]

## 📊 진행 상황
- 시작: 2025-01-15 14:30
- 상태: 진행중 (2/5 완료, 40%)
- 예상 완료: 15:30
- 다음 업데이트: 14:35 (5분 후)

## 📈 상세 작업 계획

### Task 1: JWT 토큰 생성 로직 구현
- **상태**: ✅ 완료 (14:32-14:35, 3분)
- **수정 대상**: 
  - `src/services/auth.service.ts` - JWT 토큰 생성/검증 메서드 추가
  - `src/types/auth.types.ts` - 토큰 페이로드 타입 정의
- **구현 내용**:
  ```typescript
  // 추가된 주요 메서드
  generateToken(userId: string): string
  verifyToken(token: string): TokenPayload
  ```
- **기대 효과**: 사용자 인증 시 안전한 토큰 발급 가능

### Task 2: 인증 미들웨어 구현
- **상태**: ⏳ 진행중 (14:35~)
- **수정 대상**:
  - `src/middleware/auth.middleware.ts` - 토큰 검증 미들웨어 생성
  - `src/routes/api.routes.ts` - 보호된 라우트에 미들웨어 적용
- **구현 계획**:
  - Bearer 토큰 추출 로직
  - 토큰 유효성 검증
  - 사용자 정보를 request 객체에 추가
- **기대 효과**: 모든 보호된 API 엔드포인트에 인증 적용

### Task 3: 로그인 엔드포인트 구현
- **상태**: ⏸️ 대기중
- **수정 대상**:
  - `src/controllers/auth.controller.ts` - 로그인/로그아웃 엔드포인트
  - `src/routes/auth.routes.ts` - 인증 라우트 추가
- **구현 계획**:
  - POST /auth/login - 사용자 인증 및 토큰 발급
  - POST /auth/logout - 토큰 무효화 (선택적)
  - GET /auth/me - 현재 사용자 정보 조회
- **기대 효과**: 클라이언트가 안전하게 인증 가능

### 문서 업데이트 (새 팀원 관점)
- **상태**: ⏸️ 대기중
- **업데이트 대상**:
  - `docs/api/authentication.md` - 인증 플로우 설명
  - `docs/setup/environment.md` - JWT_SECRET 환경변수 설정
- **작성 내용**:
  - 인증 토큰 획득 방법
  - API 요청 시 토큰 포함 방법
  - 인증 오류 처리 가이드

## 📝 실행 로그
### [14:32] 작업 시작
- 전체 인증 시스템 설계 검토
- JWT 라이브러리 선택 (jsonwebtoken)

### [14:35] Task 1 완료
- auth.service.ts에 토큰 생성/검증 로직 구현
- 단위 테스트 작성 및 통과

### [14:37] 이슈 자체 해결
- 🔧 문제: TypeScript 타입 오류 - Request 객체에 user 속성 없음
- 🔍 원인: Express Request 타입 확장 필요
- ✅ 해결: types/express.d.ts 파일 생성하여 Request 인터페이스 확장
- ⏱️ 지연: 2분

## ⚠️ 주의사항 및 리스크
1. **보안 고려사항**
   - JWT_SECRET은 반드시 환경변수로 관리
   - 토큰 만료 시간 적절히 설정 (현재: 24시간)
   
2. **하위 호환성**
   - 기존 API 클라이언트 영향도 검토 필요
   - 점진적 마이그레이션 전략 수립

3. **성능 영향**
   - 모든 API 요청에 토큰 검증 오버헤드 추가
   - 캐싱 전략 검토 필요

## 💡 인사이트 및 개선 제안
- refresh token 구현 고려 (보안 강화)
- rate limiting 추가 검토 (brute force 공격 방지)
- 토큰 블랙리스트 관리 시스템 필요
```

## 🛠️ 실행 규칙

### 자동 진행
- 기술적 오류는 자체 해결하고 계속 진행
- 중대 결정이나 요구사항 확인 필요시만 중단

### 오류 자체 해결 예시
| 오류 | 해결 |
|------|------|
| 컴파일 에러 | 타입/import 수정 |
| 테스트 실패 | 구현/테스트 조정 |
| 패키지 없음 | npm install/대체 |
| 권한 오류 | 경로 변경/우회 |

### 중단 조건
1. **중대 결정**: 아키텍처, 비즈니스 로직, 외부 서비스 선택
2. **요구사항 불명확**: 명세 없는 기능, 모호한 규칙
3. **제품 사양**: UX 결정, 우선순위, 범위 확인

## ⚡ 체크리스트
✅ TodoWrite로 모든 작업 등록 (리마인더 포함)
✅ plan-output.md 생성
✅ 5분마다 업데이트 리마인더 TODO 추가
✅ 각 작업 시작/완료 시 상태 업데이트
✅ "새 팀원에게 필요한가?" 기준으로 문서 TODO 추가

---

**실행**: 계획 수립 → TodoWrite로 작업 등록(리마인더 포함) → plan-output.md 생성 → "계획대로 진행하겠습니다" → 자동 실행